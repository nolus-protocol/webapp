# Nolus Backend Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# Server Configuration
# =============================================================================
HOST=0.0.0.0
PORT=3000

# Logging level (trace, debug, info, warn, error)
RUST_LOG=nolus_backend=info,tower_http=info

# =============================================================================
# External API URLs (Public - No Authentication Required)
# =============================================================================

# ETL API for protocol data, currencies, prices, leases
ETL_API_URL=https://etl-internal.nolus.network

# Skip API for cross-chain swaps
SKIP_API_URL=https://api.skip.money
# Optional: Skip API key for higher rate limits
SKIP_API_KEY=

# =============================================================================
# Chain RPC/REST Endpoints
# =============================================================================

# Nolus chain endpoints
NOLUS_RPC_URL=https://rpc.nolus.network
NOLUS_REST_URL=https://lcd.nolus.network

# Other chains for IBC/cross-chain operations
OSMOSIS_RPC_URL=https://rpc.osmosis.zone
NEUTRON_RPC_URL=https://rpc.neutron.org
# SOLANA_RPC_URL=

# =============================================================================
# Authenticated APIs (Required for full functionality)
# =============================================================================

# Referral system API
REFERRAL_API_URL=
REFERRAL_API_TOKEN=

# Zero Interest Payments API
ZERO_INTEREST_API_URL=
ZERO_INTEREST_API_TOKEN=

# =============================================================================
# Intercom Integration
# =============================================================================

# Intercom app ID for customer support widget
INTERCOM_APP_ID=hbjifswh

# Intercom secret key for identity verification (HMAC)
# Required for secure user identification
INTERCOM_SECRET_KEY=

# =============================================================================
# Cache Configuration
# =============================================================================

# Price cache TTL (seconds) - shorter for real-time data
CACHE_PRICES_TTL_SECS=30

# Config cache TTL (seconds) - longer for stable data
CACHE_CONFIG_TTL_SECS=300

# APR cache TTL (seconds)
CACHE_APR_TTL_SECS=300

# Maximum cache entries
CACHE_MAX_ENTRIES=10000

# =============================================================================
# Admin API Configuration
# =============================================================================

# Enable admin API endpoints (cache management, stats)
ADMIN_API_ENABLED=false

# Admin API key for authentication (generate a secure random string)
# Example: openssl rand -base64 32
ADMIN_API_KEY=

# =============================================================================
# Protocol Configuration
# =============================================================================

# Admin contract address (manages protocol registry)
ADMIN_CONTRACT=nolus1gurgpv8savnfw66lckwzn4zk7fp394lpe667dhu7aw48u40lj6jsqxf8nd

# Dispatcher contract address
DISPATCHER_CONTRACT=nolus14hj2tavq8fpesdwxxcu44rty3hh90vhujrvcmstl4zr3txmfvw9s0k0puz

# Active protocols (comma-separated)
# ACTIVE_PROTOCOLS=OSMOSIS-OSMOSIS-USDC_NOBLE,OSMOSIS-OSMOSIS-ALL_BTC
